@import "partials/_playlist"
@import "partials/_shops"
@import "partials/_login"

body {
	background: url(../imgs/bg.jpg) center center no-repeat fixed;
	background-size: cover;
}

h1,h2,h3,h4,h5,h6 {
	font-weight: normal;
}

.wrapper {
	width: 700px;
	margin: 0 auto;
	padding: 1rem;
	background: rgba(255,255,255, 0.9);
}

header {
	h1 {
		font-weight: bold;
	}
}